# Task Manager Configuration

app:
  name: "Task Manager"
  version: "0.1.0"
  # TODO: Add debug mode flag
  # TODO: Add log level setting

storage:
  path: "tasks.json"
  # TODO: Add backup_enabled and backup_path settings
  auto_save: true

display:
  date_format: "%Y-%m-%d %H:%M"
  # TODO: Add color_enabled setting
  # TODO: Add max_title_length setting
  table_width: 80
